{
  "name": "voicecoach",
  "private": true,
  "version": "0.0.0",
  "description": "Monorepo do Voicecoach — apps/ e packages/ em pnpm workspaces; backend Python em backend/ (uv).",
  "packageManager": "pnpm@11.2.2",
  "engines": {
    "node": ">=26"
  },
  "scripts": {
    "format": "biome format --write .",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "typecheck": "pnpm -r --if-present run typecheck",
    "gates": "pnpm run lint && pnpm run typecheck"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "@biomejs/biome": "^2.5.10"
  }
}
